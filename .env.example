# =========================
# DATABASE
# =========================
POSTGRES_DB=lite_db
POSTGRES_USER=lite_user
POSTGRES_PASSWORD=super_password_segura
POSTGRES_HOST=db
POSTGRES_PORT=5432

DATABASE_URL=postgres://lite_user:super_password_segura@db:5432/lite_db

# =========================
# DJANGO
# =========================
DJANGO_SECRET_KEY=django-super-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,backend

# =========================
# BACKEND
# =========================
BACKEND_PORT=8000

# =========================
# EMAIL (Optional - for sending inventory reports)
# =========================
# For development, use console backend (prints to console)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# For production, uncomment and configure SMTP or use external service
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-password
# EMAIL_USE_TLS=True
# DEFAULT_FROM_EMAIL=noreply@example.com

# =========================
# AI/LLM (Optional - for AI-powered recommendations in PDF)
# =========================
# LLM provider (currently supports 'huggingface')
LLM_PROVIDER=huggingface
# API key for Hugging Face (get from https://huggingface.co/settings/tokens)
LLM_API_KEY=
# Model to use for text generation (e.g., 'facebook/bart-large-cnn' for summarization)
LLM_MODEL=facebook/bart-large-cnn

# =========================
# FRONTEND
# =========================
REACT_APP_API_URL=http://localhost:8000
